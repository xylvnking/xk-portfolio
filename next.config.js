/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  
}

module.exports = nextConfig

module.exports = {
  images: {
    domains: ['cdna.artstation.com', 'cdnb.artstation.com', 'images.unsplash.com', 'mj-gallery.com', 'media1.giphy.com', 'media2.giphy.com', 'media3.giphy.com'],
  }
}